\section{Аналитическая часть}
\subsection{Анализ предметной области}

\subsubsection{Физические основы и математическое моделирование}
Туман представляет собой атмосферное явление, возникающее из-за скопления мелких капель воды в атмосфере.
Взаимодействие света с частицами тумана описывается двумя основными типами рассеяния: рассеянием Рэлея и рассеянием Ми.
Рассеяние Рэлея происходит, когда размер рассеивающих частиц значительно меньше длины волны падающего света.
Интенсивность такого рассеяния пропорциональна $\lambda^{-4}$, где~$\lambda$~--- длина волны света~\cite{rayleigh-scatter}.
Рассеяние Ми используется в случаях, когда размер частиц сопоставим с длиной волны или превышает её, что характерно для капель воды в тумане.


Для описания поведения света в среде используется уравнение переноса излучения~\cite{rte-pbr}, представленное на формуле~\ref{eq:rte}
\begin{equation}
\label{eq:rte}
(\omega \cdot \nabla_x)L(x, \omega) = -\sigma_t L(x, \omega) + \sigma_s L_s(x, \omega) + L_e(x, \omega),    
\end{equation}
где $L(x, \omega)$~--- яркость в точке~$x$ по направлению~$\omega$, $\sigma_t = \sigma_a + \sigma_s$~--- коэффициент затухания (сумма коэффициентов поглощения~$\sigma_a$ и рассеяния~$\sigma_s$), $L_s(x, \omega)$~--- яркость, полученная за счёт рассеяния внутрь среды, $L_e(x, \omega)$~--- собственное излучение среды.

Первый член правой части описывает затухание света, второй~--- рассеяние, третий~--- излучение самой среды.

Затухание света при прохождении через однородную среду описывается законом Бера-Ламберта~\cite{beer-fog}:
\begin{equation}
    \label{eq:beer-fog}
    I(d) = I_0 \exp(-\sigma_t d),
\end{equation}

где $I_0$~--- начальная интенсивность света, $I(d)$~--- интенсивность после прохождения расстояния~$d$ через среду, $\sigma_t$~--- коэффициент затухания.

Направленность рассеяния описывается фазовой функцией~$p(\omega_i, \omega_o)$, определяющей вероятность того, что свет, падающий по направлению~$\omega_i$, будет рассеян в направлении~$\omega_o$. Для изотропного рассеяния $p = 1/(4\pi)$. Для анизотропного рассеяния часто используется фазовая функция Хеньи-Гринстейна, приведена на формуле~\ref{eq:greenshtein}:
\begin{equation}
    \label{eq:greenshtein}
    p_{\text{HG}}(\cos\theta, g) = \frac{1}{4\pi} \frac{1 - g^2}{(1 + g^2 - 2g\cos\theta)^{3/2}},    
\end{equation}
Где $\theta$~--- угол рассеяния, $g \in [-1, 1]$~--- параметр анизотропии ($g > 0$~--- преимущественно прямое рассеяние, $g < 0$~--- обратное, $g = 0$~--- изотропное).

\subsubsection{Задачи визуализации и применение}

Визуализация тумана требует решения множества взаимосвязанных задач.
Физическая корректность достигается путём численного решения уравнения переноса излучения методом Монте-Карло~\cite{monte-carlo}, что требует трассировки множества лучей и вычисления интегралов по всем направлениям~\cite{rte-pbr}.
Вычислительная эффективность является важным фактором, так как объемный рендеринг требует вычисления множества точек взаимодействия для каждого луча. Использование структур ускорения, таких как иерархия ограничивающих объемов~(BVH), позволяет сократить время поиска пересечений.

Моделирование неравномерности тумана достигается за счет применения процедурных методов генерации плотности на основе шума Перлина~\cite{perlin-noise},
что создаёт реалистичные области с различной концентрацией частиц. Для неравномерного тумана плотность описывается функцией $\rho(x, y, z)$, и коэффициенты затухания становятся пространственно-зависимыми: $\sigma_t(x) = \sigma_{t,0} \cdot \rho(x, y, z)$.

В практических приложениях используются упрощённые модели: линейный туман, где затухание линейно зависит от расстояния,
экспоненциальный туман, соответствующий закону Бера-Ламберта,
и экспоненциальный квадратный туман, обеспечивающий более плавное затухание~\cite{fog-article}.
Однако для достижения фотореализма необходима реализация объемного рендеринга с решением уравнения переноса излучения.

\subsection{Методы моделирования тумана}

Существуют различные подходы к моделированию тумана в компьютерной графике, которые можно разделить на две категории: равномерный туман с постоянной плотностью и неравномерный туман с пространственно-зависимой плотностью~\cite{fog-article}.

\subsubsection{Атмосферное затухание}

При использовании атмосферного затухания плотность частиц во всем объеме тумана постоянна.
Коэффициент тумана~$f$ определяет степень видимости объекта, где $f = 1$ соответствует отсутствию тумана, а~$f = 0$~--- полному поглощению.
Итоговый цвет вычисляется по формуле~\ref{eq:atmosphere_fog}.
\begin{equation}
    \label{eq:atmosphere_fog}
    C_{\text{final}} = f \cdot C_{\text{obj}} + (1 - f) \cdot C_{\text{fog}},    
\end{equation}
где $C_{\text{obj}}$~--- цвет объекта, $C_{\text{fog}}$~--- цвет тумана.

\textbf{Линейный туман.} Интенсивность линейно зависит от расстояния~\cite{fog-opengl}:
\begin{equation}
    \label{eq:linear_fog}
    f = \frac{d_{\text{end}} - d}{d_{\text{end}} - d_{\text{start}}},    
\end{equation}
\noindent где $d$~--- расстояние до точки, $d_{\text{start}}$ и $d_{\text{end}}$~--- границы действия тумана.
Модель проста в реализации, но не соответствует физическим законам.

\textbf{Экспоненциальный туман.} Основан на законе Бера-Ламберта и является упрощенной физической моделью затухания~\cite{fog-article}:
\begin{equation}
    \label{eq:exponent}
    f = \exp(-\rho \cdot d),    
\end{equation}
\noindent где $\rho \in [0, 1]$~--- плотность тумана.

\textbf{Экспоненциальный квадратный туман.} Обеспечивает более резкое затухание на больших расстояниях~\cite{fog-article}:

\begin{equation}
    \label{eq:exponent_square}
    f = \exp(-\rho^2 \cdot d^2).
\end{equation}

\subsubsection{Объемный туман}

Объёмный туман представляет собой метод визуализации, при котором туман моделируется как трёхмерная среда,
заполняющая определённый объём пространства, а не как простой экранный эффект.
В отличие от постобработки изображения, объёмный туман физически взаимодействует с лучами света,
создавая реалистичные эффекты рассеяния и затухания~\cite{gpu-pro6}.
Для моделирования реалистичных неоднородностей применяется шум Перлина~--- градиентная функция,
генерирующая псевдослучайные значения с плавными переходами~\cite{perlin-noise}.

\begin{equation}
   \text{turbulence}(p, n) = \sum_{i=0}^{n-1} \frac{|\text{noise}(2^i \cdot p)|}{2^i}, 
\end{equation}
\noindent где $p = (x, y, z)$~--- точка в пространстве, $n$~--- количество октав.

Плотность неравномерного тумана определяется по формуле~(\ref{eq:nonconstant-p})

\begin{equation}
    \label{eq:nonconstant-p}
    \rho(p) = \rho_0 \cdot \text{turbulence}(s \cdot p, n),
\end{equation}
\noindent где $\rho_0$~--- базовая плотность, $s$~--- масштабный коэффициент.

Для моделирования приземного тумана добавляется зависимость от высоты, формула~(\ref{eq:ground-fog}):

\begin{equation}
    \label{eq:ground-fog}
    \rho(p) = \rho_0 \cdot \text{turbulence}(s \cdot p, n) \cdot \exp(-k \cdot \max(y, 0)),
\end{equation}
\noindent где $k > 0$~--- коэффициент спада по высоте.

\subsubsection{Сравнительный анализ и выбор методов}

В таблице~\ref{tab:fog-comparison} приведено сравнение методов моделирования тумана.

\begin{table}[H]
\captionsetup{justification=raggedright,singlelinecheck=false}
\caption{Сравнение методов моделирования тумана}
\label{tab:fog-comparison}
\resizebox{\linewidth}{!}{%
\begin{tabular}{|l|c|c|c|c|}
\hline
\textbf{Критерий} & \textbf{Линейный} & \textbf{Экспонен.} & \textbf{Экспонен. кв.} & \textbf{Объемный} \\
\hline
Физическая корректность & Низкая & Высокая & Средняя & Высокая \\
\hline
Вычисл. сложность & Очень низкая & Низкая & Низкая & Высокая \\
\hline
Реалистичность & Низкая & Средняя & Средняя & Высокая \\
\hline
Неоднородность & Нет & Нет & Нет & Да \\
\hline
\end{tabular}%
}
\end{table}

Для дальнейшей работы была выбрана модель на основе шума Перлина для неравномерного тумана,
а также создана модификация с <<коэффициентом спада по высоте>> для приземлённого тумана.

\section{Модель освещения}

Существует несколько подходов к генерации изображений трёхмерных сцен, различающихся по принципу работы, вычислительной сложности и качеству результата~\cite{raster-vs-rt, rendering-comparison}.

\subsection{Растеризация}

Растеризация проецирует геометрические примитивы (треугольники) на плоскость изображения и определяет, какие пиксели покрываются каждым примитивом~\cite{raster-vs-rt, real-time-rendering}. Для каждого пикселя вычисляется цвет с использованием локальных моделей освещения. Метод обеспечивает высокую производительность ($60+$ FPS), но не поддерживает физически корректные отражения, преломления и глобальное освещение~\cite{raster-vs-rt}.

\subsection{Реймарчинг}

Реймарчинг (англ.~ray marching) применяется для рендеринга объектов, заданных функциями знакового расстояния (SDF, Signed Distance Function)~\cite{ray-marching, sphere-tracing}. Алгоритм итеративно продвигает луч вдоль направления на расстояние, равное SDF в текущей точке~\cite{sphere-tracing}:

\begin{equation}
t_{i+1} = t_i + d_{\text{scene}}(\mathbf{p}_i), \quad \mathbf{p}_i = \mathbf{o} + t_i \mathbf{d},
\end{equation}

\noindent где $\mathbf{o}$~--- начало луча, $\mathbf{d}$~--- направление, $d_{\text{scene}}(\mathbf{p})$~--- SDF сцены в точке~$\mathbf{p}$. Метод эффективен для фракталов, объёмных данных и конструктивной геометрии, но медленнее традиционной трассировки лучей для полигональных моделей~\cite{ray-marching-vs-rt}.

\subsection{Обратная трассировка лучей}

Обратная трассировка лучей (англ.~backward ray tracing) испускает лучи из камеры через каждый пиксель и рекурсивно вычисляет отражения и преломления~\cite{raster-vs-rt}. При пересечении с объектом луч может расщепляться на отражённый и преломлённый компоненты. Метод обеспечивает физически корректные зеркальные отражения и прозрачность, но вычислительно затратен для глобального освещения~\cite{path-tracing-lecture}.

\subsection{Трассировка путей}

Трассировка путей (англ.~path tracing) решает уравнение рендеринга методом Монте-Карло~\cite{kajiya-rendering, path-tracing-lecture}:

\begin{equation}
L_o(\mathbf{x}, \omega_o) = L_e(\mathbf{x}, \omega_o) + \int_{\Omega} f_r(\mathbf{x}, \omega_i, \omega_o) L_i(\mathbf{x}, \omega_i) (\mathbf{n} \cdot \omega_i) \, d\omega_i,
\end{equation}

\noindent где $L_o$~--- исходящая яркость, $L_e$~--- излучение, $f_r$~--- BRDF, $L_i$~--- входящая яркость, $\mathbf{n}$~--- нормаль, $\Omega$~--- полусфера~\cite{kajiya-rendering}.

Для каждого пикселя испускается множество лучей, которые случайным образом отражаются от поверхностей до попадания на источник света или до превышения максимальной глубины~\cite{path-tracing-lecture, pbr-book}. Направление отражения выбирается методом importance sampling с учётом BRDF материала:

\begin{equation}
\omega_i \sim p(\omega_i | \mathbf{n}),
\end{equation}

\noindent где $p(\omega_i | \mathbf{n})$~--- функция плотности вероятности, например, косинусно-взвешенная для диффузных поверхностей~\cite{pbr-book}.

Метод обеспечивает фотореалистичное качество с корректным глобальным освещением, мягкими тенями и непрямым светом, но требует большого количества лучей на пиксель для снижения шума~\cite{path-tracing-lecture}.

\subsection{Объёмный рендеринг}

Для визуализации участвующих сред (туман, дым) применяется объёмный рендеринг~\cite{rte-scratchapixel, volumetric-fog}. При трассировке луча через объём расстояние до взаимодействия определяется стохастически~\cite{pbr-book}:

\begin{equation}
t = -\frac{\ln(\xi)}{\sigma_t},
\end{equation}

\noindent где $\xi \sim U(0, 1)$~--- случайная величина, $\sigma_t$~--- коэффициент затухания. В точке взаимодействия луч либо поглощается, либо рассеивается в новом направлении согласно фазовой функции~\cite{volumetric-fog}.

\subsection{Модели освещения}

Для локального расчёта освещения широко применяются эмпирические модели Фонга и Блинна~--- Фонга~\cite{phong-model, blinn-phong}.

\textbf{Модель Фонга} описывает интенсивность отражённого света как сумму трёх компонентов~\cite{phong-model}:

\begin{equation}
I = I_a k_a + I_l k_d (\mathbf{N} \cdot \mathbf{L}) + I_l k_s (\mathbf{R} \cdot \mathbf{V})^n,
\end{equation}

\noindent где $I_a$~--- интенсивность фонового освещения, $k_a$~--- коэффициент ambient, $I_l$~--- интенсивность источника, $k_d$~--- диффузный коэффициент, $k_s$~--- зеркальный коэффициент, $\mathbf{N}$~--- нормаль, $\mathbf{L}$~--- направление на источник света, $\mathbf{R}$~--- направление отражённого луча, $\mathbf{V}$~--- направление на наблюдателя, $n$~--- показатель блеска (shininess).

Вектор отражения вычисляется как~\cite{phong-model}:

\begin{equation}
\mathbf{R} = 2(\mathbf{N} \cdot \mathbf{L})\mathbf{N} - \mathbf{L}.
\end{equation}

\textbf{Модель Блинна~--- Фонга} использует вектор-полусумму~$\mathbf{H}$ вместо вектора отражения, что вычислительно эффективнее~\cite{blinn-phong}:

\begin{equation}
I = I_a k_a + I_l k_d (\mathbf{N} \cdot \mathbf{L}) + I_l k_s (\mathbf{N} \cdot \mathbf{H})^n,
\end{equation}

\noindent где вектор-полусумма определяется как:

\begin{equation}
\mathbf{H} = \frac{\mathbf{L} + \mathbf{V}}{|\mathbf{L} + \mathbf{V}|}.
\end{equation}

Модель Блинна~--- Фонга даёт более реалистичные блики при меньших вычислительных затратах и широко используется в графических API~\cite{blinn-phong, lighting-models}.

\subsection{Сравнительный анализ и выбор метода}

В таблице~\ref{tab:rendering-comparison} приведено сравнение методов рендеринга.

\begin{table}[H]
\centering
\caption{Сравнение методов рендеринга}
\label{tab:rendering-comparison}
\begin{tabular}{|l|c|c|c|c|}
\hline
\textbf{Метод} & \textbf{Физ. корр.} & \textbf{Сложность} & \textbf{Глоб. осв.} & \textbf{Объёмы} \\
\hline
Растеризация & Низкая & Очень низкая & Нет & Нет \\
\hline
Реймарчинг & Средняя & Высокая & Частично & Да \\
\hline
Трассировка лучей & Высокая & Средняя & Частично & Нет \\
\hline
Трассировка путей & Очень высокая & Очень высокая & Да & Да \\
\hline
\end{tabular}
\end{table}

Для данной работы выбрана трассировка путей, так как она обеспечивает физически корректное моделирование взаимодействия света с туманом, поддерживает объёмный рендеринг и позволяет достичь фотореалистичного качества~\cite{path-tracing-lecture, pbr-book, volumetric-fog}. Высокая вычислительная сложность компенсируется применением многопоточной обработки и структур ускорения.

\section*{Вывод}

В данной части рассмотрены основные методы рендеринга: растеризация, реймарчинг, обратная трассировка лучей, трассировка путей и объёмный рендеринг. Описаны модели локального освещения Фонга и Блинна~--- Фонга. Выполнен сравнительный анализ и обоснован выбор трассировки путей для визуализации тумана.

\clearpage




\clearpage
